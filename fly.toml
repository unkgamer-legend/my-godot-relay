app = 'my-godot-relay'
primary_region = 'iad'

[build]

[env]
  PORT = '8080'

[[services]]
  internal_port = 8080
  protocol = 'tcp'
  auto_start_machines = true
  min_machines_running = 0

  [[services.ports]]
    port = 8080
